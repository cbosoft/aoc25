# Day 9
`bash`

Just pure `bash`. I'm gonna try not to use other commands like `grep`, `awk` or `sed`.

# Part 1
Find the largest rectangle (AABB) with BL and TL corners picked from a list (the input).

# Part 2
???
